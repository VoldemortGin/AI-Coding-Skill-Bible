[package]
name = "domain"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[dependencies]
serde.workspace = true
thiserror.workspace = true
# 不可变约束:domain 保持零厂商 SDK 依赖(check_conformance 强制)。
# 只放纯类型、trait 与边界错误,不放任何 provider 实现。
