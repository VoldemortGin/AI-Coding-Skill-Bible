org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.caching=true
org.gradle.parallel=true
# The build targets Java 21 via `options.release`; it must run on a JDK 21+ (JAVA_HOME).
# google-java-format and Error Prone run on the daemon JVM, so JAVA_HOME must be 21..25+.
