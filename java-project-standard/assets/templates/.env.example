# Keys / overrides for real providers (the default Mock needs none).
# env overrides configs/settings.json: `APP_` prefix, `__` for nesting (see kernel/AppConfig.java).
# APP_LLM_PROVIDER=mock
# APP_RETRIEVER__TOP_K=5
# APP_RETRIEVER__RERANK_MODEL=mock-rerank
# OPENAI_API_KEY=
