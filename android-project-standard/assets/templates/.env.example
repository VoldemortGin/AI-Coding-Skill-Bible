# 真实 provider 的密钥与覆盖(默认 Mock 不需要)。
# env 覆盖 configs/settings.json:`APP_` 前缀,`__` 分隔嵌套(见 kernel/Config.kt)。
# APP_LLM_PROVIDER=mock
# APP_RETRIEVER__TOP_K=5
# APP_RETRIEVER__RERANK_MODEL=mock-rerank
# OPENAI_API_KEY=
